subscription($receiverId: Int!, $teamId: Int!) {
  newDirectMessage(receiverId: $receiverId, teamId: $teamId) {
    id
    text
    receiverId
    createdAt
    user {
      id
      username
    }
  }
}
