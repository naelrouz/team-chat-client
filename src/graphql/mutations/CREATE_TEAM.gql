mutation($name: String!) {
  createTeam(name: $name) {
    status
    team {
      id
      name
      channels {
        id
        name
      }
    }
    errors {
      path
      message
    }
  }
}
