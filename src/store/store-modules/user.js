const state = {
  name: 'Иван'
};

const getters = {
  name(state) {
    return state.name;
  }
};

const mutations = {};
const actions = {};

export default {
  state,
  getters,
  mutations,
  actions
};
